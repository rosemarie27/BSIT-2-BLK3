class BasicWidgetsHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Basic Widgets Home'),
      ),
      body: Column(
        children: [
          Icon(Icons.abc_outlined),
          Icon(Icons.access_alarm_outlined),

        ],
      ),
    );
  }
}
