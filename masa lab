import 'package:flutter/material.dart';


class labExam extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: LabExamHome(),
    );
  }
}



class LabExamHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        toolbarHeight: 80.0, // Optional: change the height if needed
        flexibleSpace: Center(
          child: Text(
            'Lab Examination',
            style: TextStyle(
              fontSize: 20, // You can adjust the font size
              fontWeight: FontWeight.bold, // You can adjust the font weight
            ),
          ),
        ),
      ),
      body: Center(
        child: Card(
          color: Colors.red,
          child: Column(
            mainAxisSize: MainAxisSize.min,
            children: [
              CircleAvatar(
                radius: 50,
                backgroundImage: NetworkImage('https://letsenhance.io/static/8f5e523ee6b2479e26ecc91b9c25261e/1015f/MainAfter.jpg'), // Load your network image here
              ),
              SizedBox(height: 20), // Space between the image and text
              Text('Masa John Phillip D.'),
              Text('Bachelor of Science in information Technology'),
            ],
          ),
        ),
      ),
    );
  }
}
